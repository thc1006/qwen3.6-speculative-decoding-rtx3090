__pycache__/
*.pyc
.venv/
*.log
# bench evidence: per-prompt llama-cli output and verbose dumps must survive
# `git clean` and round-trips through `git add -A`.
!v2_3090_followup/**/*.log
.DS_Store
social/

# tests/mutate.py writes this only while an older in-place run is recovering
.mutate-in-progress
